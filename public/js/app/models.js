(function() {
  define(["app", "../models/base", "../models/user", "../models/match", "../models/game", "../models/pick"], function(App, Base, User, Match, Game, Pick) {
    var Models;

    Models = App.Models;
    return Models;
  });

}).call(this);

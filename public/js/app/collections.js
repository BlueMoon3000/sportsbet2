(function() {
  define(["app", '../collections/base', "../collections/users", "../collections/games", "../collections/picks"], function(App, Base, Users, Games, Picks) {
    var Collections;

    Collections = App.Collections;
    return Collections;
  });

}).call(this);

define ["app", "../models/base", "../models/user", "../models/match", "../models/game", "../models/pick"], (App, Base, User, Match, Game, Pick) -> 
  Models = App.Models

  #Models.BaseModel = Base
  #Models.User = User
  #Models.Match = Match
  return Models